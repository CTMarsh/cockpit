apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cockpit

resources:
  - namespace.yaml
  - pvc.yaml
  - rbac.yaml
  - nginx-configmap.yaml
  - services.yaml
  - api-deployment.yaml
  - web-deployment.yaml
  - ingress.yaml
